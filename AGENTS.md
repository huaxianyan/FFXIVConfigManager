# AGENTS.md

## 语言

始终使用简体中文回答，代码、命令和专有名词除外。

## 项目定位

本项目是 FFXIV 角色配置管理工具的全新实现。旧项目 `huaxianyan/ff14-ccmt` 仅作为需求和行为参考，不复制其代码、构建产物、日志或用户数据。

## 已确定的技术方向

- 使用 C# 和当前受支持的 .NET LTS 版本；
- 使用 Avalonia UI 构建桌面界面，不使用 WPF、Electron 或 WebView 架构；
- 首期正式支持 Windows，但 Domain、Application 和通用 Infrastructure 不得依赖 Windows 专有 API；
- 为未来 Linux 和 macOS 支持保留平台抽象，未验证的平台能力不得宣称为已支持；
- 采用模块化单体和 MVVM，避免微服务及不必要的框架和抽象；
- 备份使用开放、可校验的格式，核心目标是版本化、完整性验证和失败回滚。

## FFXIV 运行状态

- 不得因为检测到 FFXIV、XIVLauncher 或相关进程正在运行而禁止备份、迁移或恢复；
- 进程状态只能用于信息展示、日志和诊断；
- 使用文件级稳定性检查、操作前恢复点、暂存写入、哈希校验、事务日志和失败回滚处理并发修改风险；
- 不得强制结束、注入或操控游戏进程。

## 实现原则

- UI 不直接执行文件系统操作，业务由 Application 用例编排；
- Domain 不依赖 UI、操作系统、数据库或具体文件系统实现；
- 备份内容的事实来源是备份及其 Manifest，索引或缓存必须可重建；
- 不解析或修改未充分验证的 FFXIV 二进制 DAT 内部结构，第一阶段只进行文件级管理；
- 默认不收集聊天日志等隐私数据；
- 所有覆盖操作必须可预览、可验证并具有明确恢复路径；
- 对文件事务、损坏备份、并发修改、取消和故障回滚编写自动化测试。
